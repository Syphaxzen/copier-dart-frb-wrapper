name: {{ package_name }}_example_cli
description: CLI example demonstrating {{ package_name }} library usage
publish_to: 'none'
version: 1.0.0

environment:
  sdk: {{ dart_sdk_version }}

dependencies:
  {{ package_name }}:
    path: ../

dev_dependencies:
  lints: ^6.0.0
